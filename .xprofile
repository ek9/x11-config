#!/bin/bash
# ~/.xprofile - start your Xorg apps here

if [ -f ~/.local/share/x230.icm ]; then
    exec dispwin -d 1 ~/.local/share/x230.icm &
fi

#exec ~/.local/bin/monitor-setup.sh &
exec nitrogen --restore &

exec setxkbmap -model pc104 -layout us,lt -option grp:alt_shift_toggle &
exec fbpanel &

exec nm-applet &

# udiskie
#exec udiskie &

# Start GNOME Keyring
#eval $(/usr/bin/gnome-keyring-daemon --start --components=pkcs11,secrets)
# export gnome-keyring variables
#export GNOME_KEYRING_CONTROL GNOME_KEYRING_PID

# notification daemon
#exec /usr/lib/notification-daemon-1.0/notification-daemon &

# blueman applet
#exec blueman-applet &

# pasystray
#exec pasystray &

# terminals
#exec termite --class="term-system" &
#exec termite --class="term-kapma" &

# scudcloud
#exec scudcloud &

# megatools
#if [ ! -d "${HOME}/var/mega" ]; then
#    mkdir -p ${HOME}/var/mega
#fi
#if which megafs 2>&1 >/dev/null; then
#    if [ ! "$(pidof megafs)"  ]; then
#        exec megafs $HOME/var/mega &
#    fi
#fi

#compton
#exec compton -b


